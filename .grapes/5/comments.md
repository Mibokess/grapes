### matt — 2026-02-27
Noticed this while looking at #1. The `Markdown` widget is already imported in `detail.py:8` and used for descriptions, so using it for comments is trivial once the parser is fixed.
