Create the `user_preferences` table with columns for theme, language, and notification settings.

## Schema

- id (primary key)
- user_id (foreign key)
- theme (text)
- language (text)
- notifications_enabled (boolean)
